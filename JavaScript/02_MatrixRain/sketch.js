function setup(){
    createCanvas(
        window.innerWidth,
        window.innerHeight
    );
    background(10);
}
function draw(){
    // draw on functio jota kutsutaan loopissa, 60 frames per second
}